#include <stdio.h>
int main()
{
    int a = 4, b = 6;
    if (a >= b)
    {

        printf("the larger number is %d", a);
    }
    else
    {
        printf("the larger number is %d", b);
    }
    return 0;
}